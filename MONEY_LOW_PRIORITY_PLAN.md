# Money 低优先级阶段方案

更新日期：2026-07-09

## 定位

Money / 每日记账不是不重要，而是现在不适合扩功能。它记录的是个人财务数据，所以低优先级阶段只做一件事：保证数据安全、可导出、可恢复、出错时不误导用户。

一句话原则：先让账本可靠，再谈分析能力。

## 1. 数据安全

当前阶段只关注这些能力：

- 导出：用户随时能把当前账本导出成 JSON 备份。
- 备份：导出前、清空等高风险操作前，应明确当前可用的备份边界；不能把未验证的自动备份写成既有能力。
- 恢复：历史本地账本或 JSON 仅作为用户手动保存、导出/导入的兼容或应急恢复材料；不承诺自动迁移、自动合并或覆盖恢复。
- 误删兜底：删除、清空这类高风险动作，要明确范围；能先备份就先备份，不能恢复就不要暗示可以撤销。

不做“看起来很安全”的文案。界面只能承诺系统已经验证过的能力。

## 2. 同步可靠性

正式 CloudBase 数据与历史本地/Supabase 材料可能不一致。处理原则：

- 当前正式主线只认 Rocky 中央账号 + CloudBase 的 owner-scoped 数据；它们是唯一正式账号和数据真相源。
- 历史本地账本和 Supabase 仅是冻结的历史材料，不再是活跃同步、自动回退或自动合并目标。
- 不自动迁移、合并或覆盖历史材料；需要恢复时，只能走显式、手动、可审计的导出/导入流程，并在实施前单独验证。
- 中央身份或 CloudBase 不可用时应失败关闭、清楚提示；不能把历史本地数据包装成正式运行时回退。
- 发现历史数据差异时，先保留用户持有的导出材料，再决定是否另行开展受控恢复验证。

低优先级阶段不做复杂迁移或冲突解决系统，只保证不把冻结历史材料误导成正式数据源、不串数据、不误导用户。

## 3. 用户边界

这个 App 只服务个人记账：

- 记录每天花了多少钱。
- 看当月花费。
- 必要时设置支出上限。
- 能导出个人记录；跨设备使用以中央账号和 CloudBase 正式数据为准。

它不是家庭账本、投资工具、财务分析系统、银行流水聚合器，也不是多人协作账本。

## 4. 最小下一步

只建议做 1-2 个动作：

1. 做一次“导出 / 清空本月 / 中央身份或 CloudBase 失败关闭”的手工验证清单，确认界面不承诺不存在的回退或恢复能力。
2. 如要支持历史材料恢复，先设计并验证独立的手动导入流程，再决定是否把“导入恢复”入口展示给用户。

第二步必须等第一步确认后再做。

## 5. 暂时不要做

- 不加新预算能力。
- 不加投资、资产、净值、理财相关功能。
- 不加多人账本、家庭账本、共享账本。
- 不加 AI 财务分析、自动分类、消费建议。
- 不迁移数据库。
- 不删除 Supabase 历史数据。
- 不重构 CloudBase 数据结构。

除非 CTO 后续明确确认，否则 Money 保持低优先级，只做安全、备份、恢复和可靠性。
